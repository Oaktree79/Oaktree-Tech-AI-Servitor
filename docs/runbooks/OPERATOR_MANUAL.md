# Operator Manual

Common commands:

```bash
serviter . analyze
serviter . submit "analyze project"
serviter . approve <task_id>
serviter . run-once
serviter-prod --root . metrics
serviter-prod --root . isolation-status
```
