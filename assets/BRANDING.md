# Branding Assets

- `ai-serviter-logo.svg` is included.
- Convert to `.ico` for Windows installers:
  - Use ImageMagick: `magick ai-serviter-logo.svg ai-serviter.ico`
- Convert to `.icns` for macOS using iconutil or a packaging tool.
